apiVersion: v1
kind: Service
metadata:
  name: ctdl-xtra-api
  namespace: ctdl-xtra
  labels:
    app.kubernetes.io/name: ctdl-xtra
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: ctdl-xtra
    environment: staging
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ctdl-xtra
    app.kubernetes.io/component: api
  ports:
    - name: http
      port: 80
      targetPort: http
