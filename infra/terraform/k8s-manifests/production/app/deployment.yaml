apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctdl-xtra-api
  namespace: ctdl-xtra
  labels:
    app.kubernetes.io/name: ctdl-xtra
    app.kubernetes.io/component: api
    app.kubernetes.io/part-of: ctdl-xtra
    environment: prod
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: ctdl-xtra
      app.kubernetes.io/component: api
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ctdl-xtra
        app.kubernetes.io/component: api
        app.kubernetes.io/part-of: ctdl-xtra
        environment: prod
    spec:
      serviceAccountName: ctdl-xtra
      nodeSelector:
        nodepool: app
      # Spread the 2 replicas across the 2 app nodes so a node loss keeps one up
      # (intra-AZ HA). Soft so a single-node window during maintenance still runs.
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: ctdl-xtra
              app.kubernetes.io/component: api
      containers:
        - name: api
          image: 996810415034.dkr.ecr.us-east-1.amazonaws.com/ctdl-xtra/api:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: RUN_MIGRATIONS
              value: "false"
            - name: LOG_LEVEL
              value: info
            - name: NODE_EXTRA_CA_CERTS
              value: /etc/ssl/certs/rds-global-bundle.pem
          envFrom:
            - secretRef:
                name: ctdl-xtra-app-env
          readinessProbe:
            httpGet:
              path: /up
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /up
              port: http
            initialDelaySeconds: 60
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          startupProbe:
            httpGet:
              path: /up
              port: http
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 18
          volumeMounts:
            - name: extraction-files
              mountPath: /data/extractions
            - name: rds-ca-bundle
              mountPath: /etc/ssl/certs/rds-global-bundle.pem
              subPath: rds-global-bundle.pem
              readOnly: true
          resources:
            requests:
              cpu: 500m
              memory: 768Mi
            limits:
              cpu: "1"
              memory: 1536Mi
      volumes:
        - name: extraction-files
          persistentVolumeClaim:
            claimName: ctdl-xtra-files
        - name: rds-ca-bundle
          configMap:
            name: rds-ca-bundle
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctdl-xtra-worker
  namespace: ctdl-xtra
  labels:
    app.kubernetes.io/name: ctdl-xtra
    app.kubernetes.io/component: worker
    app.kubernetes.io/part-of: ctdl-xtra
    environment: prod
spec:
  replicas: 2
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: ctdl-xtra
      app.kubernetes.io/component: worker
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app.kubernetes.io/name: ctdl-xtra
        app.kubernetes.io/component: worker
        app.kubernetes.io/part-of: ctdl-xtra
        environment: prod
    spec:
      serviceAccountName: ctdl-xtra
      nodeSelector:
        nodepool: app
      # Spread the 2 replicas across the 2 app nodes so a node loss keeps one up
      # (intra-AZ HA). Soft so a single-node window during maintenance still runs.
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: ScheduleAnyway
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: ctdl-xtra
              app.kubernetes.io/component: worker
      terminationGracePeriodSeconds: 60
      containers:
        - name: worker
          image: 996810415034.dkr.ecr.us-east-1.amazonaws.com/ctdl-xtra/worker:${IMAGE_TAG}
          imagePullPolicy: IfNotPresent
          env:
            - name: LOG_LEVEL
              value: info
            - name: NODE_EXTRA_CA_CERTS
              value: /etc/ssl/certs/rds-global-bundle.pem
          envFrom:
            - secretRef:
                name: ctdl-xtra-app-env
          volumeMounts:
            - name: extraction-files
              mountPath: /data/extractions
            - name: rds-ca-bundle
              mountPath: /etc/ssl/certs/rds-global-bundle.pem
              subPath: rds-global-bundle.pem
              readOnly: true
          resources:
            requests:
              cpu: 750m
              memory: 1536Mi
            limits:
              cpu: "1500m"
              memory: 2560Mi
      volumes:
        - name: extraction-files
          persistentVolumeClaim:
            claimName: ctdl-xtra-files
        - name: rds-ca-bundle
          configMap:
            name: rds-ca-bundle
