# TEST runs on a single consolidated node group (nodepool: app), no HA.
replicas: 1

resources:
  requests:
    cpu: 100m
    memory: 200Mi
  limits:
    cpu: 200m
    memory: 400Mi

nodeSelector:
  nodepool: app
